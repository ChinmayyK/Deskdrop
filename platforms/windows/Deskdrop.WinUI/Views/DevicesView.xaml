<Page
    x:Class="Deskdrop.WinUI.Views.DevicesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Deskdrop.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Connected Devices" FontSize="28" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBlock Text="Manage your paired devices and view their live telemetry status." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="14"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="DeviceTargetsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Spacing="16"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" CornerRadius="16" Padding="24">
                            <StackPanel>
                                <!-- Header -->
                                <Grid Margin="0,0,0,24">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding friendly_name}" FontSize="20" FontWeight="SemiBold"/>
                                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0">
                                            <TextBlock Text="Connected" Foreground="#1AAB29" FontSize="13" FontWeight="Medium"/>
                                            <TextBlock Text="â€¢" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13"/>
                                            <TextBlock Text="{Binding ip_address}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="Disconnect" BorderThickness="0" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="8"/>
                                </Grid>

                                <!-- Battery -->
                                <StackPanel Margin="0,0,0,24">
                                    <Grid Margin="0,0,0,8">
                                        <TextBlock Text="Battery" FontSize="13" FontWeight="Medium" HorizontalAlignment="Left"/>
                                        <TextBlock Text="85%" FontSize="13" FontWeight="Bold" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Border Height="8" CornerRadius="4" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                        <Border Width="200" Height="8" CornerRadius="4" Background="#1AAB29" HorizontalAlignment="Left"/>
                                    </Border>
                                </StackPanel>

                                <!-- Storage -->
                                <StackPanel>
                                    <Grid Margin="0,0,0,8">
                                        <TextBlock Text="Storage" FontSize="13" FontWeight="Medium" HorizontalAlignment="Left"/>
                                        <TextBlock Text="112 GB available" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Border Height="8" CornerRadius="4" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" Margin="0,0,0,12">
                                        <Grid>
                                            <Border Width="120" Height="8" CornerRadius="4,0,0,4" Background="#0A84FF" HorizontalAlignment="Left"/>
                                            <Border Width="60" Height="8" Background="#FF9500" HorizontalAlignment="Left" Margin="120,0,0,0"/>
                                            <Border Width="30" Height="8" Background="#FF2D55" HorizontalAlignment="Left" Margin="180,0,0,0"/>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>

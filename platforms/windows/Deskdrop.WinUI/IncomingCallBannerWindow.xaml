<Window
    x:Class="Deskdrop.WinUI.IncomingCallBannerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Incoming Call">

    <Grid Background="Transparent">
        <Border x:Name="AppTitleBar" Height="1" VerticalAlignment="Top" Background="Transparent" />
        
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="28" BorderThickness="1.5" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Margin="12" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Avatar -->
                <Grid Grid.Column="0" VerticalAlignment="Center">
                    <Ellipse Fill="#32ADE6" Width="44" Height="44" />
                    <TextBlock x:Name="TxtInitials" Text="D" Foreground="White" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
                
                <!-- Caller Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,16,0">
                    <TextBlock x:Name="TxtCallerName" Text="Incoming Call..." FontSize="15" FontWeight="Bold"/>
                    <TextBlock Text="Camera stream" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13"/>
                </StackPanel>
                
                <!-- Action Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">
                    <Button Width="44" Height="44" CornerRadius="22" Background="#FF3B30" Foreground="White" BorderThickness="0" Click="BtnDecline_Click" Padding="0">
                        <FontIcon Glyph="&#xE711;" FontSize="16"/>
                    </Button>
                    <Button Width="44" Height="44" CornerRadius="22" Background="#34C759" Foreground="White" BorderThickness="0" Click="BtnAccept_Click" Padding="0">
                        <FontIcon Glyph="&#xE717;" FontSize="18"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

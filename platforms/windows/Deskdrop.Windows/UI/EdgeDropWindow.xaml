<Window x:Class="Deskdrop.Windows.UI.EdgeDropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Deskdrop Edge Portal" Width="16" Height="200"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" AllowDrop="True"
        ResizeMode="NoResize" Loaded="Window_Loaded"
        DragEnter="Window_DragEnter" DragLeave="Window_DragLeave" Drop="Window_Drop" UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Grid>
        <!-- Resting Edge Sliver -->
        <Border x:Name="RestingSliver" HorizontalAlignment="Left" VerticalAlignment="Center" Width="5" Height="90" CornerRadius="2.5" Margin="2,0,0,0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#0055CC" Offset="0.0"/>
                    <GradientStop Color="#00E5FF" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#0055CC" BlurRadius="8" Opacity="0.6" ShadowDepth="0"/>
            </Border.Effect>
        </Border>

        <!-- Expanded Glass Card -->
        <Border x:Name="ExpandedCard" Width="280" Height="240" CornerRadius="16" Background="#DD1E1E24" BorderBrush="#0055CC" BorderThickness="2"
                Opacity="0" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#0055CC" BlurRadius="20" Opacity="0.5" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid Width="56" Height="56" Margin="0,0,0,14">
                    <Ellipse Fill="#400055CC" Stroke="#0055CC" StrokeThickness="2"/>
                    <iconPacks:PackIconLucide Kind="ArrowRightToLine" Width="26" Height="26" Foreground="#0055CC" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <TextBlock Text="Edge Portal Drop ✨" Foreground="White" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock x:Name="ExpandedSubText" Text="Instant transfer to active mesh" Foreground="#8CFFFFFF" FontSize="11.5" FontWeight="Medium" HorizontalAlignment="Center" Margin="12,4,12,0" TextWrapping="Wrap" MaxWidth="250" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

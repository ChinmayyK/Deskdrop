<Window x:Class="Deskdrop.Windows.CameraPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Continuity Camera Preview" Height="480" Width="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Closing="Window_Closing">

    <Window.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Stroke" Color="#E5E5EA"/>
    </Window.Resources>

    <Border Background="{StaticResource AppBackground}" CornerRadius="16" BorderBrush="{StaticResource Stroke}" BorderThickness="1" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="20" ShadowDepth="4" Direction="270"/>
        </Border.Effect>
        <Grid>
            <!-- Title Bar for Dragging -->
            <Grid Height="40" VerticalAlignment="Top" Background="Transparent" MouseLeftButtonDown="Window_MouseDown" Panel.ZIndex="10">
                <Button   FontSize="10" Width="40" Height="30" HorizontalAlignment="Right" Background="Transparent" Foreground="#8E8E93" BorderThickness="0" Click="BtnClose_Click"><iconPacks:PackIconLucide Kind="X"/></Button>
            </Grid>

            <Grid Margin="16,40,16,16">
                <Border CornerRadius="8" ClipToBounds="True" Background="#F2F2F7" BorderBrush="{StaticResource Stroke}" BorderThickness="1">
                    <Grid>
                        <Image x:Name="CameraImage" Stretch="Uniform" />
                        <TextBlock x:Name="StatusText" Text="Waiting for stream..." Foreground="#8E8E93" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Medium" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>


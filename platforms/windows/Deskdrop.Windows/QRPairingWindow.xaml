<Window x:Class="Deskdrop.Windows.QRPairingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Magic Link Pair" Height="450" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <LinearGradientBrush x:Key="AppBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0b0812" Offset="0"/>
            <GradientStop Color="#1a1130" Offset="0.5"/>
            <GradientStop Color="#0b0812" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Border Background="{StaticResource AppBackground}" CornerRadius="16" BorderBrush="#20FFFFFF" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <!-- Title Bar for Dragging -->
            <Grid Height="40" VerticalAlignment="Top" Background="Transparent" MouseLeftButtonDown="Window_MouseDown" Panel.ZIndex="10">
                <Button Content="✕" Width="30" Height="30" HorizontalAlignment="Right" Margin="0,0,10,0" Background="Transparent" Foreground="#B0B0B0" BorderThickness="0" Click="BtnClose_Click"/>
            </Grid>

            <Grid Margin="24,40,24,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Magic Link Pair" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Grid.Row="1" Text="Scan the QR code below from the Deskdrop Android app to pair instantly." 
                           Foreground="#B0B0B0" TextWrapping="Wrap" TextAlignment="Center" Margin="0,12,0,24"/>
                
                <Border Grid.Row="2" Background="White" CornerRadius="16" Padding="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="20" ShadowDepth="5"/>
                    </Border.Effect>
                    <Image x:Name="ImgQRCode" Width="220" Height="220" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>

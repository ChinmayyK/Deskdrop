<Window x:Class="Deskdrop.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:Deskdrop.Windows"
        xmlns:local2="clr-namespace:Deskdrop.Windows.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Deskdrop" Height="720" Width="960"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        AllowDrop="True" DragEnter="Grid_DragEnter" DragLeave="Grid_DragLeave" Drop="Grid_Drop"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        FontFamily="Segoe UI">

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" CaptionHeight="88" ResizeBorderThickness="24" CornerRadius="12" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="AppStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
            <local:MultiplyConverter x:Key="MultiplyConverter" />
        </ResourceDictionary>
    </Window.Resources>

    
    <Grid Background="#0C0C0E">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- Custom Caption Buttons (Top Right, overlapping content) -->
        <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,16,16,0" Panel.ZIndex="100" WindowChrome.IsHitTestVisibleInChrome="True">
            <Button Style="{StaticResource ModernButton}" Background="Transparent" Foreground="{StaticResource InkSoft}" BorderThickness="0" Width="32" Height="32" Padding="0" Click="BtnMinimize_Click" ToolTip="Minimize">
                <iconPacks:PackIconLucide Kind="Minus" Width="14" Height="14"/>
            </Button>
            <Button Style="{StaticResource ModernButton}" Background="Transparent" Foreground="{StaticResource InkSoft}" BorderThickness="0" Width="32" Height="32" Padding="0" Margin="4,0,0,0" ToolTip="Maximize">
                <iconPacks:PackIconLucide Kind="Square" Width="12" Height="12"/>
            </Button>
            <Button Style="{StaticResource ModernButton}" Background="Transparent" Foreground="{StaticResource InkSoft}" BorderThickness="0" Width="32" Height="32" Padding="0" Margin="4,0,0,0" Click="BtnClose_Click" ToolTip="Close">
                <iconPacks:PackIconLucide Kind="X" Width="14" Height="14"/>
            </Button>
        </StackPanel>
        
        <!-- ================= LEFT SIDEBAR ================= -->
        <Border Grid.Column="0" Background="#161618" BorderBrush="#20FFFFFF" BorderThickness="0,0,1,0">
            <Grid>
                
                
                <StackPanel Margin="0,60,0,0">
                    <!-- Branding -->
                    <StackPanel Orientation="Horizontal" Margin="24,0,24,24">
                        <iconPacks:PackIconLucide Kind="Zap" Width="20" Height="20" Foreground="#007AFF" Margin="0,0,12,0"/>
                        <TextBlock Text="DeskDrop" Foreground="White" FontSize="18" FontWeight="Bold"/>
                    </StackPanel>
                    
                    <!-- Workspace Navigation -->
                    <TextBlock Text="WORKSPACE" Foreground="#888888" FontSize="10" FontWeight="Bold" Margin="24,0,24,8"/>
                    
                    <StackPanel Margin="12,0,12,24">
                        <!-- Devices (Selected) -->
                        <Border Background="#007AFF" CornerRadius="6" Padding="12,8" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconLucide Kind="MonitorSmartphone" Width="16" Height="16" Foreground="White" Margin="0,0,12,0"/>
                                <TextBlock Text="Devices" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                        <!-- Clipboard -->
                        <Border Background="Transparent" CornerRadius="6" Padding="12,8" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconLucide Kind="Clipboard" Width="16" Height="16" Foreground="#BBBBBB" Margin="0,0,12,0"/>
                                <TextBlock Text="Clipboard" Foreground="#BBBBBB" FontSize="13" FontWeight="Medium"/>
                            </StackPanel>
                        </Border>
                        <!-- Transfers -->
                        <Border Background="Transparent" CornerRadius="6" Padding="12,8" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconLucide Kind="ArrowRightLeft" Width="16" Height="16" Foreground="#BBBBBB" Margin="0,0,12,0"/>
                                <TextBlock Text="Transfers" Foreground="#BBBBBB" FontSize="13" FontWeight="Medium"/>
                            </StackPanel>
                        </Border>
                        <!-- Settings -->
                        <Border Background="Transparent" CornerRadius="6" Padding="12,8" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconLucide Kind="Settings" Width="16" Height="16" Foreground="#BBBBBB" Margin="0,0,12,0"/>
                                <TextBlock Text="Settings" Foreground="#BBBBBB" FontSize="13" FontWeight="Medium"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    
                    <!-- Devices List -->
                    <TextBlock Text="DEVICES" Foreground="#888888" FontSize="10" FontWeight="Bold" Margin="24,0,24,8"/>
                    
                    <ListBox x:Name="DevicesList" Background="Transparent" BorderThickness="0" Margin="12,0,12,0" 
                             SelectedItem="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=SelectedPeer, Mode=TwoWay}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#007AFF" CornerRadius="6" Padding="12,8" Margin="0,2">
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="8" Height="8" Fill="#1AAB29" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding DisplayName}" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- ================= CENTER WORKSPACE ================= -->
        <Grid Grid.Column="1" Background="#0C0C0E">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Top Search Bar -->
            <Border Grid.Row="0" Margin="40,24,40,0" Background="#161618" CornerRadius="8" BorderBrush="#20FFFFFF" BorderThickness="1" Height="40" WindowChrome.IsHitTestVisibleInChrome="True">
                <Grid>
                    <iconPacks:PackIconLucide Kind="Search" Width="16" Height="16" Foreground="#888888" Margin="16,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <TextBox Background="Transparent" Foreground="White" BorderThickness="0" Padding="44,0,16,0" VerticalContentAlignment="Center" FontSize="14" Text="Search Files, Clipboard, Devices..."/>
                </Grid>
            </Border>
            
            <!-- Main Content Scroll -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,40,0,0">
                <StackPanel Margin="40,0,40,80">
                    
                    <!-- Hero Header -->
                    <TextBlock Text="Your ecosystem, perfectly balanced." Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,0,0,12"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,40">
                        <Ellipse Width="8" Height="8" Fill="#1AAB29" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Connected over Local Wi-Fi." Foreground="#BBBBBB" FontSize="14"/>
                    </StackPanel>
                    
                    <!-- Launchpad -->
                    <TextBlock Text="Launchpad" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <UniformGrid Columns="3" Margin="-10,0,-10,40">
                        <!-- Transfer Files -->
                        <Border Background="#161618" CornerRadius="12" Margin="10,0,10,20" Padding="0,32" Cursor="Hand" WindowChrome.IsHitTestVisibleInChrome="True" MouseLeftButtonDown="Launchpad_TransferFiles_Click">
                            <StackPanel>
                                <Border Width="56" Height="56" CornerRadius="28" Background="#1A007AFF" HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <iconPacks:PackIconLucide Kind="Send" Width="24" Height="24" Foreground="#007AFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Transfer Files" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Browse Device -->
                        <Border Background="#161618" CornerRadius="12" Margin="10,0,10,20" Padding="0,32" Cursor="Hand" WindowChrome.IsHitTestVisibleInChrome="True" MouseLeftButtonDown="Launchpad_BrowseDevice_Click">
                            <StackPanel>
                                <Border Width="56" Height="56" CornerRadius="28" Background="#1A7856FF" HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <iconPacks:PackIconLucide Kind="HardDrive" Width="24" Height="24" Foreground="#7856FF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Browse Device" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Clipboard -->
                        <Border Background="#161618" CornerRadius="12" Margin="10,0,10,20" Padding="0,32" Cursor="Hand" WindowChrome.IsHitTestVisibleInChrome="True">
                            <StackPanel>
                                <Border Width="56" Height="56" CornerRadius="28" Background="#1AFF2D55" HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <iconPacks:PackIconLucide Kind="Clipboard" Width="24" Height="24" Foreground="#FF2D55" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Clipboard" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Speed Test -->
                        <Border Background="#161618" CornerRadius="12" Margin="10,0,10,20" Padding="0,32" Cursor="Hand" WindowChrome.IsHitTestVisibleInChrome="True">
                            <StackPanel>
                                <Border Width="56" Height="56" CornerRadius="28" Background="#1A00B4D8" HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <iconPacks:PackIconLucide Kind="Gauge" Width="24" Height="24" Foreground="#00B4D8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Speed Test" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Remote Control -->
                        <Border Background="#161618" CornerRadius="12" Margin="10,0,10,20" Padding="0,32" Cursor="Hand" WindowChrome.IsHitTestVisibleInChrome="True">
                            <StackPanel>
                                <Border Width="56" Height="56" CornerRadius="28" Background="#1AFF9500" HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <iconPacks:PackIconLucide Kind="MousePointer2" Width="24" Height="24" Foreground="#FF9500" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Remote Control" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Settings -->
                        <Border Background="#161618" CornerRadius="12" Margin="10,0,10,20" Padding="0,32" Cursor="Hand" WindowChrome.IsHitTestVisibleInChrome="True">
                            <StackPanel>
                                <Border Width="56" Height="56" CornerRadius="28" Background="#1A8E8E93" HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <iconPacks:PackIconLucide Kind="Settings" Width="24" Height="24" Foreground="#8E8E93" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Settings" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Magic Link Pair -->
                        <Border Background="#161618" CornerRadius="12" Margin="10,0,10,20" Padding="0,32" Cursor="Hand" WindowChrome.IsHitTestVisibleInChrome="True" MouseLeftButtonDown="Launchpad_MagicLinkPair_Click">
                            <StackPanel>
                                <Border Width="56" Height="56" CornerRadius="28" Background="#1A1AAB29" HorizontalAlignment="Center" Margin="0,0,0,16">
                                    <iconPacks:PackIconLucide Kind="QrCode" Width="24" Height="24" Foreground="#1AAB29" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Magic Link Pair" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                    
                    <!-- Recent Activity -->
                    <TextBlock Text="Recent Activity" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                    <Border Background="#161618" CornerRadius="12" Padding="24">
                        <TextBlock Text="No recent activity." Foreground="#888888" FontSize="14"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        
        <!-- ================= RIGHT SMART DEVICE PANEL ================= -->
        <Border Grid.Column="2" Background="#161618" BorderBrush="#20FFFFFF" BorderThickness="1,0,0,0" DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=SelectedPeer}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Visibility="{Binding Converter={StaticResource NullToVisibilityConverter}}">
                <StackPanel Margin="32,60,32,40">
                    
                    <!-- Header -->
                    <TextBlock Text="{Binding DisplayName}" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,40">
                        <Ellipse Width="8" Height="8" Fill="#1AAB29" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding status}" Foreground="#1AAB29" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text=" Â· Wi-Fi Â· " Foreground="#888888" FontSize="12"/>
                        <TextBlock Text="{Binding ip_address}" Foreground="#888888" FontSize="12"/>
                    </StackPanel>
                    
                    <!-- Battery -->
                    <StackPanel Margin="0,0,0,32">
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Battery" Foreground="White" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                            <TextBlock Text="{Binding battery_percentage, StringFormat={}{0}%}" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Border Height="8" CornerRadius="4" Background="#333333">
                            <Border Width="{Binding battery_percentage, Converter={StaticResource MultiplyConverter}, ConverterParameter=2.56}" Height="8" CornerRadius="4" Background="#1AAB29" HorizontalAlignment="Left"/>
                        </Border>
                    </StackPanel>
                    
                    <!-- Storage -->
                    <StackPanel Margin="0,0,0,32">
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Storage" Foreground="White" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                            <TextBlock Text="Dynamic Storage Binding" Foreground="#BBBBBB" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Border Height="8" CornerRadius="4" Background="#333333" Margin="0,0,0,12">
                            <Grid>
                                <!-- Storage bars will be fully implemented in Step 4 Telemetry -->
                                <Border Width="120" Height="8" CornerRadius="4,0,0,4" Background="#FF9500" HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                        
                        <!-- Storage Legend -->
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="6" Height="6" Fill="#FF9500" Margin="0,0,6,0"/>
                            <TextBlock Text="Images" Foreground="#BBBBBB" FontSize="10" Margin="0,0,16,0"/>
                            
                            <Ellipse Width="6" Height="6" Fill="#FF2D55" Margin="0,0,6,0"/>
                            <TextBlock Text="Videos" Foreground="#BBBBBB" FontSize="10" Margin="0,0,16,0"/>
                            
                            <Ellipse Width="6" Height="6" Fill="#8E8E93" Margin="0,0,6,0"/>
                            <TextBlock Text="Other" Foreground="#BBBBBB" FontSize="10"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- Pairing Actions -->
                    <StackPanel Margin="0,0,0,32">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowConnectButton}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding pairingRequested}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding outgoingPairingWaiting}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        
                        <TextBlock Text="Pairing" Foreground="White" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ShowConnectButton}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <Button Content="Pair Device" Background="#007AFF" Foreground="White" BorderThickness="0" Padding="16,8" Cursor="Hand" Click="BtnPairDevice_Click"/>
                        </StackPanel>
                        
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding pairingRequested}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="Wants to pair with this PC" Foreground="#FF9500" FontSize="12" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Accept" Background="#1AAB29" Foreground="White" BorderThickness="0" Padding="16,8" Margin="0,0,8,0" Cursor="Hand" Click="BtnAcceptPairing_Click"/>
                                <Button Content="Decline" Background="#FF3B30" Foreground="White" BorderThickness="0" Padding="16,8" Cursor="Hand" Click="BtnDeclinePairing_Click"/>
                            </StackPanel>
                        </StackPanel>
                        
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding outgoingPairingWaiting}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="Waiting for response..." Foreground="#FF9500" FontSize="12" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                    
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
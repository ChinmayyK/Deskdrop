<Window x:Class="Deskdrop.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Deskdrop.Windows"
        Title="Deskdrop" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" CaptionHeight="32" CornerRadius="12" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Colors & Brushes -->
        <SolidColorBrush x:Key="BrandElectric" Color="#0055CC"/>
        <SolidColorBrush x:Key="BrandViolet" Color="#5E5CE6"/>
        <SolidColorBrush x:Key="BrandCyan" Color="#32ADE6"/>
        <SolidColorBrush x:Key="SurfaceElevated" Color="#251b3d"/>
        <SolidColorBrush x:Key="SurfaceStrong" Color="#10ffffff"/>
        <SolidColorBrush x:Key="Surface" Color="#0b0812"/>
        <SolidColorBrush x:Key="Ink" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="InkSoft" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="InkSubtle" Color="#707070"/>
        <SolidColorBrush x:Key="Stroke" Color="#20FFFFFF"/>

        <LinearGradientBrush x:Key="AppBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0b0812" Offset="0"/>
            <GradientStop Color="#1a1130" Offset="0.5"/>
            <GradientStop Color="#0b0812" Offset="1"/>
        </LinearGradientBrush>

        <!-- Styles -->
        <Style x:Key="SidebarButton" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" CornerRadius="8" Padding="12,8" Margin="0,4" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AnimatedCardStyle" TargetType="Border">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.15">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent" Cursor="Hand">
                            <Border x:Name="border" Width="44" Height="24" CornerRadius="12" Background="#3A3A3C" Padding="2" VerticalAlignment="Center">
                                <Ellipse x:Name="thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform X="0"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Border>
                            <ContentPresenter Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BrandElectric}"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="Checked">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="20" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Unchecked">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="0" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrandElectric}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Cursor="Hand">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3377FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border CornerRadius="3" Background="#44FFFFFF" Margin="2,0"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" Style="{StaticResource ModernScrollBar}" HorizontalAlignment="Right" Minimum="0" Maximum="{TemplateBinding ScrollableHeight}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" CornerRadius="8" Background="Transparent" Margin="0,0,0,8" Padding="16,12">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Container -->
    <Border Background="{StaticResource AppBackground}" CornerRadius="12" BorderBrush="{StaticResource Stroke}" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <!-- Glowing Blobs for Glassmorphism -->
            <Ellipse Width="450" Height="450" Fill="#225E5CE6" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-100,-100,0,0">
                <Ellipse.Effect>
                    <BlurEffect Radius="150"/>
                </Ellipse.Effect>
            </Ellipse>
            <Ellipse Width="400" Height="400" Fill="#1A32ADE6" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-50,-50">
                <Ellipse.Effect>
                    <BlurEffect Radius="120"/>
                </Ellipse.Effect>
            </Ellipse>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="1"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource SurfaceStrong}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- App Icon / Title -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,40,20,20" WindowChrome.IsHitTestVisibleInChrome="True">
                        <TextBlock Text="Deskdrop" Foreground="{StaticResource Ink}" FontSize="18" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Nav items -->
                    <StackPanel Grid.Row="1" Margin="12,0" WindowChrome.IsHitTestVisibleInChrome="True">
                        <RadioButton GroupName="Nav" Content="Home" Foreground="{StaticResource Ink}" Style="{StaticResource SidebarButton}" IsChecked="True" Checked="NavHome_Checked"/>
                        <RadioButton GroupName="Nav" Content="Timeline" Foreground="{StaticResource InkSoft}" Style="{StaticResource SidebarButton}" Checked="NavTimeline_Checked"/>
                        <RadioButton GroupName="Nav" Content="Devices" Foreground="{StaticResource InkSoft}" Style="{StaticResource SidebarButton}" Checked="NavDevices_Checked"/>
                    </StackPanel>

                    <!-- Settings -->
                    <StackPanel Grid.Row="2" Margin="12,20" WindowChrome.IsHitTestVisibleInChrome="True">
                        <RadioButton GroupName="Nav" Content="Settings" Foreground="{StaticResource InkSoft}" Style="{StaticResource SidebarButton}" Checked="NavSettings_Checked"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Divider -->
            <Rectangle Grid.Column="1" Fill="{StaticResource Stroke}"/>

            <!-- Content Area -->
            <Grid Grid.Column="2">
                <!-- Custom Title Bar -->
                <Grid Height="40" VerticalAlignment="Top" Background="Transparent" Panel.ZIndex="10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Content="—" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource InkSoft}" BorderThickness="0" Click="BtnMinimize_Click"/>
                        <Button Content="✕" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource InkSoft}" BorderThickness="0" Click="BtnClose_Click"/>
                    </StackPanel>
                </Grid>

                <!-- Views Container -->
                <Grid Margin="0,40,0,0">
                    
                    <!-- Home View -->
                    <Grid x:Name="HomeView" Visibility="Visible">
                        <StackPanel Margin="40">
                            <TextBlock Text="Actions" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Ink}"/>
                            <TextBlock Text="Quick tools for your ecosystem" Foreground="{StaticResource InkSoft}" Margin="0,4,0,24"/>
                            
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Push Clipboard Card -->
                                <Border Style="{StaticResource AnimatedCardStyle}" Grid.Row="0" Grid.Column="0" Background="#110055CC" CornerRadius="16" Padding="20" Margin="0,0,8,8" MouseLeftButtonDown="BorderPushClipboard_Click" Cursor="Hand">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="4"/>
                                    </Border.Effect>
                                    <Border.BorderBrush>
                                        <SolidColorBrush Color="#0055CC" Opacity="0.5"/>
                                    </Border.BorderBrush>
                                    <Border.BorderThickness>1</Border.BorderThickness>
                                    <StackPanel Background="Transparent">
                                        <TextBlock Text="📋" FontSize="24"/>
                                        <TextBlock Text="Clipboard Sync" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Ink}" Margin="0,12,0,4"/>
                                        <TextBlock Text="Send copied text &amp; images" Foreground="{StaticResource InkSoft}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Send Files Card -->
                                <Border Style="{StaticResource AnimatedCardStyle}" Grid.Row="0" Grid.Column="1" Background="#115E5CE6" CornerRadius="16" Padding="20" Margin="8,0,0,8" MouseLeftButtonDown="BorderSendFiles_Click" Cursor="Hand">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="4"/>
                                    </Border.Effect>
                                    <Border.BorderBrush>
                                        <SolidColorBrush Color="#5E5CE6" Opacity="0.5"/>
                                    </Border.BorderBrush>
                                    <Border.BorderThickness>1</Border.BorderThickness>
                                    <StackPanel Background="Transparent">
                                        <TextBlock Text="📁" FontSize="24"/>
                                        <TextBlock Text="Files" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Ink}" Margin="0,12,0,4"/>
                                        <TextBlock Text="Share files across devices" Foreground="{StaticResource InkSoft}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Stream Camera Card -->
                                <Border Style="{StaticResource AnimatedCardStyle}" Grid.Row="1" Grid.Column="0" Background="#1132ADE6" CornerRadius="16" Padding="20" Margin="0,8,8,0" MouseLeftButtonDown="BorderStreamCamera_Click" Cursor="Hand">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="4"/>
                                    </Border.Effect>
                                    <Border.BorderBrush>
                                        <SolidColorBrush Color="#32ADE6" Opacity="0.5"/>
                                    </Border.BorderBrush>
                                    <Border.BorderThickness>1</Border.BorderThickness>
                                    <StackPanel Background="Transparent">
                                        <TextBlock Text="📷" FontSize="24"/>
                                        <TextBlock Text="Stream Camera" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Ink}" Margin="0,12,0,4"/>
                                        <TextBlock Text="View connected camera feed" Foreground="{StaticResource InkSoft}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <!-- Broadcast Camera Card -->
                                <Border Style="{StaticResource AnimatedCardStyle}" Grid.Row="1" Grid.Column="1" Background="#11FF3B30" CornerRadius="16" Padding="20" Margin="8,8,0,0" MouseLeftButtonDown="BorderBroadcastCamera_Click" Cursor="Hand" x:Name="BorderBroadcastCamera">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="4"/>
                                    </Border.Effect>
                                    <Border.BorderBrush>
                                        <SolidColorBrush Color="#FF3B30" Opacity="0.5"/>
                                    </Border.BorderBrush>
                                    <Border.BorderThickness>1</Border.BorderThickness>
                                    <StackPanel Background="Transparent">
                                        <TextBlock Text="🎥" FontSize="24"/>
                                        <TextBlock x:Name="TxtBroadcastTitle" Text="Broadcast Camera" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Ink}" Margin="0,12,0,4"/>
                                        <TextBlock x:Name="TxtBroadcastDesc" Text="Push local webcam to peers" Foreground="{StaticResource InkSoft}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- Timeline View -->
                    <Grid x:Name="TimelineView" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Margin="40,40,40,20">
                            <TextBlock Text="Activity" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Ink}"/>
                            <TextBlock Text="Your clipboard history" Foreground="{StaticResource InkSoft}" Margin="0,4,0,0"/>
                        </StackPanel>
                        
                        <ScrollViewer Grid.Row="1" Margin="40,0,40,40">
                            <ItemsControl x:Name="TimelineList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource SurfaceStrong}" CornerRadius="12" Padding="16" Margin="0,0,0,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding TypeIcon}" FontSize="24" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Summary}" Foreground="{StaticResource Ink}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding Source}" Foreground="{StaticResource InkSoft}" FontSize="12" Margin="0,4,0,0"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="2" Text="{Binding RelativeTime}" Foreground="{StaticResource InkSubtle}" FontSize="12" VerticalAlignment="Top"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>

                    <!-- Devices View -->
                    <Grid x:Name="DevicesView" Visibility="Collapsed">
                        <StackPanel Margin="40">
                            <TextBlock Text="Ecosystem" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Ink}"/>
                            <TextBlock Text="Nearby devices" Foreground="{StaticResource InkSoft}" Margin="0,4,0,24"/>
                            
                            <ItemsControl x:Name="DevicesList" Margin="0,0,0,24">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource SurfaceStrong}" CornerRadius="12" Padding="16" Margin="0,0,0,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="💻" FontSize="24" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding friendly_name}" FontWeight="Medium" Foreground="{StaticResource Ink}"/>
                                                    <TextBlock Text="{Binding status}" Foreground="{StaticResource InkSoft}" FontSize="12"/>
                                                </StackPanel>
                                                <Button Grid.Column="2" Style="{StaticResource ModernButton}" Content="Disconnect" Background="#2AFFFFFF" Foreground="{StaticResource Ink}" Padding="12,6" Tag="{Binding device_id}" Click="BtnDisconnectDevice_Click"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <TextBlock Text="Manual Connect (Magic Link)" Foreground="{StaticResource BrandElectric}" FontWeight="SemiBold" Margin="0,0,0,16"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtConnectAddress" Style="{StaticResource ModernTextBox}" Grid.Column="0" Margin="0,0,12,0" Text="192.168.1.x:47823"/>
                                <Button Content="Connect" Style="{StaticResource ModernButton}" Grid.Column="1" Click="BtnConnect_Click" Margin="0,0,12,0"/>
                                <Button Content="Show QR Code" Style="{StaticResource ModernButton}" Grid.Column="2" Background="#2AFFFFFF" Click="BtnShowQR_Click"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- Settings View -->
                    <Grid x:Name="SettingsView" Visibility="Collapsed">
                        <ScrollViewer>
                            <StackPanel Margin="40">
                                <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Ink}"/>
                                <TextBlock Text="Deskdrop Preferences" Foreground="{StaticResource InkSoft}" Margin="0,4,0,32"/>
                                
                                <!-- General -->
                                <TextBlock Text="General" Foreground="{StaticResource BrandElectric}" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                <CheckBox x:Name="ChkSyncEnabled" Style="{StaticResource ToggleSwitchStyle}" Content="Enable clipboard syncing" Margin="0,0,0,12"/>
                                <CheckBox x:Name="ChkSyncText" Style="{StaticResource ToggleSwitchStyle}" Content="Sync text" Margin="24,0,0,12"/>
                                <CheckBox x:Name="ChkSyncImages" Style="{StaticResource ToggleSwitchStyle}" Content="Sync images" Margin="24,0,0,12"/>
                                <CheckBox x:Name="ChkSyncFiles" Style="{StaticResource ToggleSwitchStyle}" Content="Sync files" Margin="24,0,0,32"/>
                                
                                <!-- Privacy -->
                                <TextBlock Text="Privacy &amp; Security" Foreground="{StaticResource BrandElectric}" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                <CheckBox x:Name="ChkShowNotifications" Style="{StaticResource ToggleSwitchStyle}" Content="Show notifications on receive" Margin="0,0,0,12"/>
                                <CheckBox x:Name="ChkRequireTofu" Style="{StaticResource ToggleSwitchStyle}" Content="Require confirmation for new devices" Margin="0,0,0,32"/>
                                
                                <!-- Network -->
                                <TextBlock Text="Network" Foreground="{StaticResource BrandElectric}" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="200"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Device Name:" Foreground="{StaticResource Ink}" VerticalAlignment="Center"/>
                                    <TextBox x:Name="TxtDeviceName" Style="{StaticResource ModernTextBox}" Grid.Column="1"/>
                                </Grid>
                                
                                <Button Content="Save Settings" Style="{StaticResource ModernButton}" HorizontalAlignment="Left" Margin="0,24,0,0" Click="BtnSaveSettings_Click"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
